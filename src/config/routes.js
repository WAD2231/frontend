const routes = {
  dashboard: '/',
  product: '/products',
  customers: '/customers',
  analytics: '/analytics',
  supports: '/supports',
  settings: '/settings',
  seller: '/seller',
  orders: '/orders',
}

export default routes;